#include <stdio.h>

struct personla {
    char name[20];
    int day;
    char month[10];
    int year;
    float salary;
};

int main(void) {
    struct personal person;

    printf("Input Valuses\n");
    scanf("%s %d %s %d %f",
        person.name,
        &person.day,
        person.month,
        person.yaer,
        )
}